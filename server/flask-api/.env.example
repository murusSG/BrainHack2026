# Flask service
FLASK_ENV=development
PORT=8000

# Node API gateway (for service-to-service calls)
NODE_API_URL=http://localhost:3000

# Supabase — same project the Node API writes to (optional until DB is wired)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
