{
  "name": "brainhack2026",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "react-router-dom": "^7.16.0"
  }
}
